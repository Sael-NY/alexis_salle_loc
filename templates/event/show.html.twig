{% extends 'base.html.twig' %}

{% block title %}Voilà - {{ event.title }}{% endblock %}

{% block body %}

    {{ form_start(formView) }}

    {{ form_widget(formView) }}

    {{ form_end(formView) }}

    {{ event.animator.firstname }}

    {{ event.category.name }}
{% endblock %}